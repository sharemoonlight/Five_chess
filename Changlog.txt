项目日志 ：

    8月24日
	下午：
    	项目刚开始，我们采用了老师带着写的一些功能函数，屏幕的初始化函数（void lcd_init(void)），解除映射和关闭文件函数（void lcd_uninit()），
	清屏函数（void clear(int color)），触摸点函数（get_usr_touch），和一些简单的画圆和画线的函数。然后自己写了用画线函数画了一个12*12的棋盘，
	用画圆函数画了黑棋和白棋 棋盘的规格是480*480的像素点，棋子是20*20的像素点。
	晚上：
		用show_bmp函数把棋子和棋盘的位置在显示屏上成功显示出来，下一步就是找位置，把棋子放上去，规定触摸的点必须在棋盘格子内，才有效，
	第一步是先点击棋盘，表示现实下棋的拿棋子这一步，黑棋子先行，白棋子后行，棋子都是用画圆函数棋子放在格子的正中心位置，然后定义了一个数组
	int piece_position[480][480] = 0，//初始化棋盘数组,全为0，记录棋盘的每个下棋点是否下了棋子，然后根据手触摸点来判断棋子应该下在哪里  
	if((x0-x)*(x0-x)+ (y0 - y) * (y0 - y) <= 20*20 && flag == 0 && piece_position [x][y] == 0)//flag == 0 下黑棋
	if((x0-x)*(x0-x)+ (y0 - y) * (y0 - y) <= 20*20 && flag == 1 && piece_position [x][y] == 0)//flag == 1 下白棋
	//判断坐标在哪个格子，i和j就相当于记录该位置下了一颗棋子后，就把数组qipan这个数组赋值一个数(白棋子为1，黑棋子为2)
	piece_position [x][y] = 2;//黑棋的piece_position 为 2  piece_position [x][y] = 1;//白棋的piece_position 为 1
   8月25日
	上午：
		改了一下代码后，找了几张图片（800*480）做UI界面还用画图软件 画了一些提示的标语，比如下一步放什么颜色的棋子，在放完棋子后，
	提示下一步该放什么颜色的棋子的图片就会在右上角显示出来，还画了几个按键放到图片中 比如（返回游戏 退出游戏 悔棋）
	下午：
		主要写了int Game_victory()//判断输赢这个函数，用二维数组的方法去遍历，每一次下了一个棋子后，全部位置都遍历一遍，有横向、竖向、左下到右上、
	左上到右下这四个方向去判断是否有五个相同的数字，有的话就显示白/黑棋赢了这张图片，然后点击右上角，可以点击返回游戏，就会重新开始游戏，
	晚上：
		在利用晚自习的时间优化了一些代码、做了一个开始游戏的页面 ，当按下“开始游戏”按键时，会跳转到 “玩游戏”的页面，同时在 “玩游戏”的页面设置了 
	"准备开始""黑方走棋" "白方走棋"的指示、设置了("退出游戏")的按键，将“白棋盘、黑棋盘”图片更新了，想到了悔棋的思路 还没有实现代码。